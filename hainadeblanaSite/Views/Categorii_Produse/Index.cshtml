@model IEnumerable<hainadeblanaSite.Models.Categorii_Produse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NumeCategorie)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NumeCategorie)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CategorieID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CategorieID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CategorieID })
        </td>
    </tr>
}

</table>
