@model IEnumerable<HainadeblanaSite.Models.ProdusImagine>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CaleImagine)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Produs.NumeProdus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CaleImagine)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Produs.NumeProdus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ImagineID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ImagineID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ImagineID })
        </td>
    </tr>
}

</table>
